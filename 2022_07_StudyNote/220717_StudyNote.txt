<2022.07.17일자 공부 기록>

1. 생활코딩 WEB2 - JavaScript 문서 정독
	프로그램, 프로그래밍, 프로그래머 ~ UI vs API
		1) 리팩토링 -> 중복의 제거
		(코드의 간결함, 유지 보수를 위해 원 기능을 건들지 않으면서 최대한 중복을 제거하는 방향으로 가야 한다.)

		2) 객체
		(객체에는 속성(이름-값 쌍으로 이뤄짐)과 메소드(기능 담당, 함수와 동일)로 이뤄진다.)
		(객체의 속성 이름을 넣을 때 공백 포함 시 . 연산으로 접근하는 것보다 객체["속성 이름"] 으로 접근해야
		문법적 오류가 나지 않는다.)